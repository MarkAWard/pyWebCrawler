Web Crawler written in Python

At this point the crawler returns an index of all words that were found. And with each word a list of urls where
that word can be found.

TODO
Improve the list of words put into the index. 
Remove words such as: a, the, it, is...
Only get words from certain sections of the source
Implement PageRank
